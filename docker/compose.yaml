services:
  database:
    image: "postgres:18.3-alpine3.23"
    env_file:
      - ../.env
    ports:
      # Loopback only: a development database with the credentials of
      # `.env` must not be reachable from the network.
      - "127.0.0.1:5432:5432"